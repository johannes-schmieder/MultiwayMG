name: Search issue 3 sparse fixtures

on:
  push:
    branches:
      - codex/issue-3-repair-bootstrap

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  search:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/issue-3-repair-bootstrap
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.85.0"
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Format, lint, and search
        run: |
          cargo fmt --all
          cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
          cargo run --locked --release -p multiway-mg \
            --example issue3_fixture_search --all-features \
            | tee issue3-fixture-search.tsv
      - name: Commit formatter output and search evidence
        run: |
          mkdir -p benchmarks/results/2026-09-04
          cp issue3-fixture-search.tsv \
             benchmarks/results/2026-09-04/issue3-fixture-search.tsv
          rm .github/workflows/run-issue3-fixture-search.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Search sparse refinements for issue 3 challenge fixtures"
          git push origin HEAD:codex/issue-3-repair-bootstrap
