# M6l source qualification

All Rust1.85 required checks pass: format, strict Clippy, all/minimal feature
workspace tests, warning-free docs, and both unchanged automatic example units.
All 126 Python tests pass, including eleven new paired-layout tests. These cover
real timeout/protocol failures, observer exclusion, false certificates, exact
repeat/layout math/work, failed warmup/measurement charges and expanded inputs.
`checks.json` preserves commands, statuses, log hashes and changed source hashes.

No Rust/probe or dependency source differs from verified M6k main04b2184. Its
qualified debug/release schema2 executable remains the common boundary, now used
strictly uninstrumented by this separate performance policy. M6k allocation and
actual protocol receipts remain applicable; this is not a new allocation claim.

`expanded-input-preflight.json` records topology-only checks before solver
measurement for twenty family/shape combinations. All stay below100,000 tuples
and4,096 levels per factor. Prefix and mixed-zero input checks also pass.
Exact-source Rust/allocation CI35437783030/35437783035 and PR
CI35437785190/35437785213 pass every job. `source-ci.json` preserves the receipts.
Downloaded Linux/macOS/Windows debug/release logs exactly match all 39 allocation
controls and four zero-allocation denials (6,240 certified columns/configuration);
see `cross-platform-allocations.json`. This reaffirms the same unchanged contract,
not an allocator high-water measurement for the larger timing inputs.

Four [complete collections](../prepared-automatic-layout-v1/README.md) retain
51,840 processes and 505,800 certified measured columns with no failed attempts.
All 15,120 overlapping scalar processes match M6j; all 19,440 overlapping schema2
reference processes match M6k, including their layout/grouping/ABI fields. These
sets overlap and must not be added as unique observations. Expanded inputs have
no prior same-input reference; their exact repeats and within-route layout checks
pass. Copies and every archive member independently revalidate. Three Mac
summaries recompute exactly; eight Linux derived geometric means differ only
within the explicitly scoped archival tolerance. Raw/scientific fields are exact.
All four provider ZIP digests were independently verified against GitHub.

No default, calibration, holdout or competitive claim follows. Final evidence-head
review/CI and verified-main merge remain the delivery gates.

The final CI provenance failure and narrow repair are documented in
[the result document](../../../../docs/ISSUE5_AUTOMATIC_LAYOUT_ECONOMICS.md#diagnostic-provenance-repair-after-final-ci).
The failed artifact remains preserved unchanged; repaired validation accepts its
1,080 exact pairs and 29,520 certified columns. The original failed run is not
relabeled as successful. See `provenance-repair.json` for checks and hashes.
